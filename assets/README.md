# Exporting and Importing Assets

## Textures

When using textures for the project, ensure that the game uses KTX2 textures instead of the raw
image files. Image files are inserted into the repository in order for other developers to see what
the texture should look like without having to import it into the game to do so. However, they
should be converted to KTX2 instead of being used directly in the game.
